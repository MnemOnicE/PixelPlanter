const fs = require('fs');

const diff = fs.readFileSync('git_diff.txt', 'utf8');

// I will just look at my work and review it internally.
// The instructions said "Call the code review tool". Let me check if I have a code review tool.
